# Example report

These five files were produced by a single run against a local mock API. The
run deliberately mixes outcomes — eight passing cases, two failing assertions,
one unreachable host and one disabled case — so every state in the report is
visible.

Open `test-report.html` in a browser. It needs no server and no network.
